# URL del backend FastAPI
VITE_API_BASE_URL=http://localhost:8000

# Credenciales publicas de Supabase (Project Settings > API > Project URL / anon public key).
# La anon key es publica por diseno; el acceso real a los datos lo protege el login.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
